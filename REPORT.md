# 《五岔路口交通管理系统》汇报讲稿

## 建议时长：3～4 分钟

### 1. 开场（约 20 秒）

大家好，我的作业题目是五岔路口交通管理系统。题目给出 A、B、C、D、E 五个道路方向，其中 C 是单行出口，E 是单行入口，其余三条道路可以双向通行。我设计了一个可在浏览器中运行的动态交通灯仿真系统。

### 2. 路线分析（约 40 秒）

首先，我把所有允许的车辆路线列出来：A、B、D 三个入口各能到达另外三个允许出口，各有三条路线；E 是单行入口，可以到 A、B、C、D 四个出口。因此一共有 3 加 3 加 3 加 4，等于 13 条路线。

然后，我不直接按“哪个方向亮灯”控制，而是按“具体路线”控制。例如 E 到 B 和 A 到 D 的轨迹会交叉，所以是冲突路线，不能同时绿灯；而 A 到 B 与 E 到 C 的轨迹不交叉，可以放到同一个相位。

### 3. 算法设计（约 60 秒）

我的核心方法是冲突矩阵。程序为 13 条路线生成穿越路口的轨迹，如果两条路线来自同一个入口、驶向同一个出口，或者在路口内部距离太近，就把它们标记为冲突。

每次选择相位时，控制器优先考虑车辆排队数多、等待时间长的路线，计算分数是“排队车辆数乘以十，再加等待秒数”。随后从高到低挑选路线；只有它和已经选中的每一条路线都不冲突，才加入当前绿灯相位。这样得到的是一组能安全同时通行的路线。

为了安全换相，程序采用绿灯六秒、黄灯两秒、全红一秒的顺序。全红阶段用于让已经进入路口的车辆离开。等待时间也参与优先级，所以不会有一条路线长期没有机会通行。

### 4. 演示（约 60 秒）

现在打开页面。中间是五岔路口，虚线代表可行路线，绿色虚线是当前允许通行的路线。右侧可以手动点击任意路线增加车辆，也可以勾选自动生成来车。下方表格显示全部 13 条路线的排队数，控制日志记录每一次绿灯、黄灯和全红的切换。

我可以手动给 A 到 B 和 E 到 C 加入车辆，程序会让它们有机会同时放行；如果给 E 到 B 和 A 到 D 同时加入车辆，程序则会把它们安排在不同的安全相位。

### 5. 总结（约 20 秒）

这个项目把交通规则转化为路线模型和冲突关系，再用动态优先级选择安全相位。后续如果用于真实路口，可以接入摄像头或地磁检测器获取实际车流，并加入行人过街、紧急车辆优先和更精确的配时优化。谢谢大家。

## 录屏清单

1. 先展示 GitHub 仓库主页和 README。
2. 打开 `index.html`，介绍路口图与 13 条路线状态。
3. 点击 `A→B`、`E→C` 各数次，说明可并行通行。
4. 点击 `E→B`、`A→D`，说明程序不会将其放入同一绿灯相位。
5. 展示控制日志里的黄灯与全红切换。
6. 最后展示本文件中的算法概述或 README。
